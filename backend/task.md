# Task Tracker — Vision Worker High-Severity Fixes

- `[x]` **Step 1:** Priority Inference Queue (Issue 3.1 🔴)
  - `[x]` Replace single `INFERENCE_INPUT_QUEUE` with `INFERENCE_ACTIVE_QUEUE` + `INFERENCE_BACKGROUND_QUEUE`
  - `[x]` Rewrite `gpu_inference_worker()` to drain active queue first
  - `[x]` Update `run_yolo_pipeline()` to route frames to correct queue
  - `[x]` Update output queue `maxsize` at startup
- `[x]` **Step 2:** Fix ByteTrack Frame Rate (Issue 1.1 🔴)
- `[x]` **Step 3:** Add Detection Filtering (Issues 2.1 + 2.3 🟡)
  - `[x]` Add `min_bbox_area`, `min_person_ratio`, `max_person_ratio` to `DETECTION_CONFIG`
  - `[x]` Add area filter in `run_yolo_pipeline()` to reject tiny SAHI noise
  - `[x]` Add height/width ratio filter to reject columns, poles, benches, vehicles
- `[x]` **Step 4:** Fix Background Temporal Filtering (Issue 2.4 🟡)
  - `[x]` Unified active/background into single `min_age` branch (active=3, background=2)
  - `[x]` Background tracks now pruned like active (no more `track_ages = {}` wipe)
  - `[x]` False positives must survive 2× 5-second cycles (~10s) to be counted
- `[x]` **Step 5:** Eliminate Wasted CLAHE & JPEG (Issues 3.3 + 3.4 🟡)
  - `[x]` Move CLAHE from `run_yolo_pipeline()` to `camera_thread()` (only when `should_process_ai`)
  - `[x]` Change `run_yolo_pipeline()` signature to accept `frame_proc` + `frame_raw`
  - `[x]` Gate JPEG encoding: background streams only encode when AI ran (~1/5s vs 30/s)
  - `[x]` Remove `frame.copy()` default — use raw `frame` directly for background idle frames
- `[x]` **Step 6:** Eliminate Unconditional `frame.copy()` (Issue 3.2 🟡)
  - `[x]` Background+no-AI ticks now skip all rendering (overlay, JPEG) entirely
  - `[x]` Active ticks use `pipe_frame` directly from pipeline — no intermediate copy
  - `[x]` CCTV overlay + JPEG encode moved inside the `should_process_ai` block
- `[x]` **Step 7:** Multi-User Active Location Support (Architectural Enhancement)
  - `[x]` Replace `active_location_id` (int) with `active_locations` dict (location_id → heartbeat time)
  - `[x]` Locations auto-expire after 30s without a heartbeat
  - `[x]` `/set-active-location` now registers heartbeats, expires stale entries
  - `[x]` New `/deactivate-location` endpoint for immediate deactivation
  - `[x]` `/video_feed` and `/live-count` accept `?location_id=` query param
  - `[x]` `camera_thread` uses membership check (`location_id in active_locations`)
  - `[x]` `api_server.py`: `/api/yolo/initialize` no longer deactivates all other locations
  - `[x]` Frontend: 10s heartbeat interval while viewing, deactivate on switch/unmount
  - `[x]` Frontend: pass `location_id` to MJPEG stream and live-count endpoints
- `[x]` **Step 8:** Bounding Box Interpolation When GPU Is Busy (Issue 1.3 🟡)
  - `[x]` Add per-track velocity state (`track_velocities`, `prev_gpu_positions`, `last_gpu_result_time`)
  - `[x]` Compute velocity from consecutive GPU results (ignore gaps > 2s)
  - `[x]` Interpolate stale boxes on active stream using `pos + velocity × dt` (capped at 500ms)
  - `[x]` Reset interpolation state on video loop reset
- `[x]` **Step 9:** Reset Tracker State on Location Switch (Issue 1.4 🟢 Low-Med)
  - `[x]` On background→active: reset `track_ages`, `last_confirmed_detections`, interpolation state
  - `[x]` On active→background: clear `last_tracked_objects`, `last_confirmed_detections`, interpolation state
  - `[x]` Log transitions with "tracker state reset" / "state cleared" for debugging
- `[ ]` **Step 10:** Adaptive / Per-Location CLAHE (Issue 2.2 🟡)
- `[ ]` **Step 11:** Zone/ROI Masking Per Location (Issue 2.5 🟢 Low)
- `[x]` **Step 12:** Hot-Loop Cleanup (Issue 3.5 ⚪ Negligible)
  - `[x]` Move `location_high_thresholds` to module-level `LOCATION_HIGH_THRESHOLDS`
